import React from "react"

//Sub Headers for the certain Input parts

const SubHeader = props => {
    return (
        <div className="text-red-700 text-left">{props.subHeader}</div>
    )
}

export default SubHeader